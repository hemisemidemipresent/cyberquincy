module.exports = {
    name: 'update',
    execute(message) {
        return message.channel.send(
            'No one knows.\nNo one knows when the update is going to come out\nNo one knows when they will fix all the bugs\nNo one knows when the next update will come out because NK does not have a schedule'
        );
    },
};
